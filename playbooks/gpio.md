# GPIO MVP playbook

Prove the configured pin comes from the board design or board configuration. Verify device/pin registration, output low/high, input sampling, pull configuration, and interrupt edge handling when requested. Use an observable load or loopback; do not infer voltage from a software return code.
