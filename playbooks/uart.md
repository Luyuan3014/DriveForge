# UART MVP playbook

Verify registration, baud/parity/stop configuration, transmit, receive, interrupt-driven receive, loopback, and a bounded stress transfer. Capture byte counts and error counters. A transmit-only log is not RX or interrupt evidence.
