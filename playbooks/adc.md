# ADC MVP playbook

Verify device/channel registration, raw sampling, reference and scale conversion, repeated samples, and basic min/max/noise statistics. Compare against an independently known voltage; do not label a plausible raw count as calibrated accuracy.
