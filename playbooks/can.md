# CAN MVP playbook

Verify RT-Thread device registration, clock-derived bit timing, filters, transmit, receive, RX interrupt, and loopback. When an external node is available, prove ACK and bidirectional frames. Exercise bus-off recovery only with a controlled test setup. Record frame IDs, DLC, counts, error state, and loss rate.
